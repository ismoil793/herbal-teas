import React from 'react';
import "./loader.css"

const Loader = () => {
   return (
       <div className="preloader">
          <div className="pulse"></div>
       </div>
   );
};

export default Loader;